{
  "name": "@appoosto/core",
  "version": "0.1.1",
  "description": "Core types, interfaces, and utility functions for Appoosto backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "test": "jest",
    "version": "git add -A",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "appoosto",
    "core",
    "types",
    "utilities"
  ],
  "author": "Appoosto Team",
  "license": "MIT",
  "publishConfig": {
    "access": "restricted"
  },
  "peerDependencies": {
    "@nestjs/common": "^9.0.0 || ^10.0.0",
    "@nestjs/core": "^9.0.0 || ^10.0.0",
    "@nestjs/graphql": "^11.0.0 || ^12.0.0",
    "graphql": "^16.0.0"
  },
  "peerDependenciesMeta": {
    "@nestjs/common": {
      "optional": false
    },
    "@nestjs/core": {
      "optional": false
    },
    "@nestjs/graphql": {
      "optional": false
    },
    "graphql": {
      "optional": false
    }
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.6"
  }
}
